# Q. Write a program that takes as string from the user and print the number of spaces in the string


s = input("Enter a string: ")

# Count spaces
space_count = s.count(" ")

print("Number of spaces in the string:",space_count)
