a = (3, 22, 2, 13)

print(a)
print(a[2])
a[3] = 4         # Run-time error will be seen because tuples is immutable and its can't   be    changed after creation

my_tuple = (10, 20, 30)
single_element = (5,)  # Tuple with one element (comma required)
print(my_tuple[1])